# GPU-accelerated dependencies for gsc
# Install with: pip install -r requirements_gpu.txt

# Core dependencies
numpy>=1.20.0
matplotlib>=3.3.0

# GPU acceleration (choose one based on your CUDA version)
# For CUDA 11.x:
cupy-cuda11x>=10.0.0

# For CUDA 12.x (uncomment if needed):
# cupy-cuda12x>=12.0.0

# Alternative: CuPy from source (if above doesn't work)
# See: https://docs.cupy.dev/en/stable/install.html

# Optional: For profiling
# py-spy>=0.3.0
